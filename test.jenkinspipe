#!groovy
node {
  stage('Load Funds') { // <2>
        /* .. snip .. */
       echo 'build...'
    }
    stage('Calc Fund Nav') {
        /* .. snip .. */
       echo 'test'
    }
    stage('Merge Fund Announcements') {
        /* .. snip .. */
       echo 'deploy'
    }
}
