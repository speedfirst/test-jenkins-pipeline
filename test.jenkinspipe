#!groovy
node {
  stage('Build') { // <2>
        /* .. snip .. */
       echo 'build...'
    }
    stage('Test') {
        /* .. snip .. */
       echo 'test'
    }
    stage('Deploy') {
        /* .. snip .. */
       echo 'deploy'
    }
}
